{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>DateCommande</th>
                <td>{{ commande.dateCommande ? commande.dateCommande|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>IdProduit</th>
                <td>{{ commande.idProduit }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ commande.idUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_commande_index') }}">back to list</a>

    <a href="{{ path('crud_commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('crud_commande/_delete_form.html.twig') }}
{% endblock %}
