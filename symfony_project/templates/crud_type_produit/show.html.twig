{% extends 'base.html.twig' %}

{% block title %}TypeProduit{% endblock %}

{% block body %}
    <h1>TypeProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_produit.id }}</td>
            </tr>
            <tr>
                <th>Saison</th>
                <td>{{ type_produit.saison }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ type_produit.Sexe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_type_produit_index') }}">back to list</a>

    <a href="{{ path('crud_type_produit_edit', {'id': type_produit.id}) }}">edit</a>

    {{ include('crud_type_produit/_delete_form.html.twig') }}
{% endblock %}
